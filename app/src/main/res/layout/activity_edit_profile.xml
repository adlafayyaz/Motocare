<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#070A12">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="22dp">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="34dp"
            android:text="@string/edit_profile_title"
            android:textColor="#F4F5FA"
            android:textSize="28sp"
            android:textStyle="bold" />

        <TextView style="@style/FormLabel" android:text="@string/profile_name" />
        <EditText
            android:id="@+id/editProfileName"
            style="@style/FormInput"
            android:hint="@string/profile_name_hint"
            android:inputType="textCapWords" />

        <TextView style="@style/FormLabel" android:text="@string/profile_email" />
        <EditText
            android:id="@+id/editProfileEmail"
            style="@style/FormInput"
            android:hint="@string/profile_email_hint"
            android:inputType="textEmailAddress" />

        <Button
            android:id="@+id/buttonSaveProfile"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:layout_marginTop="28dp"
            android:backgroundTint="#FFCB25"
            android:text="@string/save"
            android:textColor="#070A12"
            android:textStyle="bold" />
    </LinearLayout>
</ScrollView>
